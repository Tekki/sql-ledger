Upgrading from version 2.8.24 to 2.8.25

1. install SQL-Ledger over top
   # cd /usr/local
   # tar zxvf sql-ledger-2.8.25.tar.gz

2. fix permissions
   # chown -hR nobody:nogroup *
   # chmod 711 templates users css spool

Note: replace nobody:nogroup with the webserver's user and group

3. log in

