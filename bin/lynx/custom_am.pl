1;

sub display_form {
	$form->error( $locale->text('Unauthorized!') );
}

sub edit_template {
	$form->error( $locale->text('Unauthorized!') );
}

sub save_template {
	$form->error( $locale->text('Unauthorized!') );
}
